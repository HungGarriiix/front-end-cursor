# Webhook Configuration
# URL for the AI chatbot webhook
# Should accept POST requests with { "prompt": string }
# Should return { "output": string }
VITE_WEBHOOK_URL=https://your-webhook-domain.com/webhook

# API Base URL (Optional)
# If not provided, defaults to window.location.origin
# Used for general API requests via useApiQuery and useApiMutation hooks
VITE_API_BASE_URL=https://your-api-domain.com
